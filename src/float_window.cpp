#include "info_window.h"

#include "XPLMDisplay.h"
#include "XPCWidget.h"
#include "XPStandardWidgets.h"
#include "XPLMDataAccess.h"
#include "XPLMGraphics.h"

#include <algorithm>
#include <iostream>
#include <set>

namespace SUPERSLEW {

  /*
  FloatWindow * showFloatWindow(const std::string& title, const std::string& msg0, const std::string& msg1, int x, int y, float tmr) {
    const int window_width = 320;
    const int window_height = 80;

    // x,y are the top left of window
    int x2 = x + window_width;
    int y2 = y - window_height;
    
    FloatWindow * info_window = new FloatWindow(x, y, x2, y2, title, msg0, msg1, tmr);
    info_windows.insert(info_window);
    return info_window;
  }
  */
}
    
